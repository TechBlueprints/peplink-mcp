# Copy to .env (gitignored) to run the live integration tests (pytest -m live)
# against real Peplink gear. Without these, live tests are skipped.
PEPLINK_TEST_AP_HOST=
PEPLINK_TEST_SNMP_COMMUNITY=public
